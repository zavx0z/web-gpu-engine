# Roadmap: Оптимизация производительности WebGPU-движка

## 🎯 Цель

Повысить производительность (FPS) и отзывчивость движка для рендеринга сложных сцен с тысячами объектов, устранить потенциальные "заикания" (stutter) и обеспечить максимальный параллелизм между CPU и GPU.

## 🧠 Контекст

Текущая архитектура движка уже реализует множество передовых практик:

* Пакетная запись uniform-данных (один `writeBuffer` на все объекты).
* Раздельные конвейеры для статичных и анимированных мешей (нет divergence в шейдерах).
* Эффективный бинарный поиск ключевых кадров анимации (O(log n)).
* Сортировка объектов по типу конвейера для минимизации переключений состояния.
* Кэширование геометрии.

Этот Roadmap фокусируется на дальнейших, более продвинутых оптимизациях, которые станут критичными при масштабировании сцены.

## 📈 Совокупный ожидаемый результат

После выполнения всех подзадач движок должен:

1. **Полностью избегать блокировок CPU-GPU** за счет двойной/тройной буферизации данных.
2. **Избегать обработки невидимых объектов** благодаря Frustum Culling, что снизит нагрузку на CPU и GPU.
3. **Рендерить тысячи однотипных объектов с минимальными накладными расходами** с помощью GPU Instancing.
4. **Работать без пауз сборщика мусора** в критических циклах рендеринга.
5. **Соответствовать исходному требованию "один `writeBuffer` на кадр"** после объединения uniform-буферов.
6. Показывать стабильно высокий FPS в сценах с >10k объектов и сложной анимацией.

## 🔗 Зависимости подзадач

1. `01_uniform-buffer-consolidation.md` — можно выполнять независимо.
2. `02_double-buffering-uniforms.md` — логически следует за пунктом 1, но технически независима.
3. `03_frustum-culling-implementation.md` — полностью независимая задача.
4. `04_gpu-instancing-support.md` — независимая задача, но может использовать инфраструктуру из пункта 3 для отсечения инстансов.
5. `05_object-pool-optimization.md` — независимая задача, улучшающая CPU-производительность.

Каждая задача может браться в работу отдельно и выполняется за 1-2 сеанса разработки.
