# Подзадача: Реализация Frustum Culling

## 📌 Связанные файлы проекта
*   `src/core/math/` (новые файлы: `Plane.ts`, `Frustum.ts`)
*   `src/renderer/index.ts` (интеграция в цикл рендеринга)
*   `src/scene/Scene.ts` или `src/renderer/RenderList.ts` (функция сбора объектов)
*   `src/core/Object3D.ts` (добавление bounding volume)

## 🛠 Что сделать
Добавить отсечение объектов, находящихся за пределами пирамиды видимости камеры, чтобы не отправлять их на GPU.

### Шаг 1: Создать математические классы
1.  **`Plane.ts`**: Класс, представляющий плоскость в виде `Ax + By + Cz + D = 0`.
    *   Поля: `normal: Vector3`, `constant: number`.
    *   Методы: `setFromNormalAndCoplanarPoint()`, `distanceToPoint()`, `normalize()`.
2.  **`Frustum.ts`**: Класс, содержащий массив из 6 `Plane`.
    *   Метод `setFromProjectionMatrix(matrix: Matrix4)` — извлекает плоскости из матрицы `view-projection` методом Грибба-Хартманна.
    *   Метод `intersectsSphere(sphere: Sphere): boolean` — проверка пересечения со сферой.
    *   Метод `intersectsBox(box: Box3): boolean` — проверка пересечения с AABB.

### Шаг 2: Добавить Bounding Volumes объектам
1.  В `Object3D.ts` добавить опциональные поля `boundingSphere: Sphere` и `boundingBox: Box3`.
2.  В классе `Mesh` автоматически вычислять `boundingSphere` и `boundingBox` на основе геометрии при ее загрузке/создании.
3.  Добавить метод `updateWorldBoundingVolume()` в `Object3D`, который обновляет мировой bounding volume на основе `worldMatrix`.

### Шаг 3: Интегрировать отсечение в цикл рендеринга
1.  В `render()` метода `Renderer`, после обновления матриц сцены, создать экземпляр `Frustum`.
2.  Извлечь плоскости из матрицы `camera.matrixWorldInverse * camera.projectionMatrix`.
3.  Модифицировать функцию `collectSceneObjects` (или аналог) так, чтобы она принимала `Frustum` как параметр.
4.  Внутри функции, для каждого `Mesh`, проверять `frustum.intersectsSphere(mesh.worldBoundingSphere)`.
    *   Если `false` — пропустить объект и всех его детей.
    *   Если `true` — добавить объект в `renderList`.

### Шаг 4: Оптимизация (опционально, но рекомендуется)
1.  Использовать иерархические структуры (например, BVH) для групп объектов.
2.  Для статических объектов кэшировать результат проверки отсечения до тех пор, пока камера или объект не переместились значительно.

## ✅ Ожидаемый результат
*   В сложной сцене с объектами, разбросанными за пределами экрана, `renderList` содержит только видимые объекты.
*   Нагрузка на GPU (количество вызовов отрисовки, обработанных вершин) снижается пропорционально объему сцены за пределами камеры.
*   Производительность (FPS) возрастает, особенно в сценах с большим количеством объектов.
*   Визуально рендеринг не изменяется (отсеченные объекты просто не видны).