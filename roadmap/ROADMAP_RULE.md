# Правило формирования Roadmap

## 📁 Структура

Roadmap состоит из:

1. **Общего файла** `overall.md` — описывает цель, контекст и совокупный ожидаемый результат.
2. **Подзадач в отдельных `.md`-файлах** — каждая подзадача:
   - Сохраняется как отдельный файл с понятным именем (например, `batch-uniform-write.md`).
   - Привязана к конкретным исходным файлам проекта.
   - Содержит пошаговые инструкции «Что сделать».
   - Завершается блоком «Ожидаемый результат».

## 🎯 Требования к подзадачам

- **Конкретика**: Каждая подзадача должна быть выполнимой одним разработчиком за один сеанс.
- **Атомарность**: Подзадача не должна зависеть от других подзадач (если возможно).
- **Файловая привязка**: Чётко указаны все файлы, которые нужно изменить или создать.
- **Избегание дублирования**: Если две подзадачи логически связаны (например, шейдер + пайплайн), они объединяются в одну.

## 🔄 Процесс актуализации

- При обнаружении логической связи между подзадачами — объединять их в одну более полную задачу.
- Удалять старые файлы подзадач после объединения.
- Все файлы roadmap хранятся в одной директории без подпапок.

## ✅ Итог

Roadmap должен быть:

- **Понятным** для любого участника команды.
- **Выполнимым** по частям.
- **Синхронизированным** с текущей архитектурой проекта.
