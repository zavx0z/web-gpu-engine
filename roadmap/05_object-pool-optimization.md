# Подзадача: Оптимизация выделения памяти через Object Pool

## 📌 Связанные файлы проекта
*   `src/core/ObjectPool.ts` (новый утилитарный класс)
*   `src/renderer/index.ts` (замена создания временных объектов на пулы)
*   `src/animation/` (если есть временные объекты в вычислениях анимации)
*   `src/scene/` (оптимизация функций обхода иерархии)

## 🛠 Что сделать
Устранить паузы сборщика мусора (GC) в "горячих" циклах рендеринга за счет повторного использования временных объектов (Vector3, Matrix4, Quaternion).

### Шаг 1: Создать универсальный класс `ObjectPool<T>`
1.  Реализовать в `src/core/ObjectPool.ts`.
2.  Интерфейс:
    ```typescript
    class ObjectPool<T> {
        constructor(factory: () => T, initialSize: number = 0);
        acquire(): T; // Возвращает объект из пула или создает новый
        release(obj: T): void; // Возвращает объект в пул
        clear(): void;
    }
    ```

### Шаг 2: Инициализировать пулы в рендерере
1.  В конструкторе `Renderer` создать статические пулы для часто используемых типов:
    ```typescript
    private static _vector3Pool = new ObjectPool(() => new Vector3(), 50);
    private static _matrix4Pool = new ObjectPool(() => new Matrix4(), 20);
    private static _quaternionPool = new ObjectPool(() => new Quaternion(), 20);
    ```
2.  Добавить публичные статические геттеры для доступа.

### Шаг 3: Рефакторинг "горячих" функций
Найти в коде рендерера и анимации функции, которые создают временные объекты в цикле. Например:
1.  `updateSceneUniforms` — создает `Vector3` для преобразования позиций света.
2.  `renderMesh` / `renderLines` — могут создавать временные матрицы или векторы.
3.  Функции обхода сцены (`traverse`).

**Паттерн рефакторинга:**
```typescript
// Было:
const tempVec = new Vector3();
// ... вычисления с tempVec

// Стало:
const tempVec = Renderer.vector3Pool.acquire();
// ... вычисления с tempVec
Renderer.vector3Pool.release(tempVec); // ВАЖНО: вернуть сразу после использования!
```

### Шаг 4: Провести аудит и тестирование
1.  Запустить движок с включенным профилированием памяти в DevTools.
2.  Убедиться, что в ходе рендеринга не происходит постоянного роста памяти и частых сборок мусора (массивные "пилы" на графике).
3.  Проверить, что производительность (FPS) стала более стабильной, без случайных проседаний (stutter).

## ✅ Ожидаемый результат
*   На графике выделения памяти в DevTools больше нет "пилообразного" паттерна, характерного для постоянного создания и удаления мелких объектов.
*   Сборки мусора происходят реже и не во время критичных циклов рендеринга.
*   FPS стал более стабильным, исчезли микро-проседания (stutter), особенно заметные на низких мощностях CPU или мобильных устройствах.
*   Код "горячих" путей рендеринга не создает новых экземпляров `Vector3`, `Matrix4` и т.д.